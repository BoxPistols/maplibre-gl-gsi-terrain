---
description: Build the library and example
---

Build the library and example application:

```bash
pnpm build && pnpm build:example
```

If the build fails, analyze the errors and suggest fixes.
